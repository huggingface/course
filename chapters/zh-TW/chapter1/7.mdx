<!-- DISABLE-FRONTMATTER-SECTIONS -->

# 快速測驗

<CourseFloatingBanner
    chapter={1}
    classNames="absolute z-10 right-0 top-0"
/>

到目前為止，本章節涵蓋了很多內容！如果你沒有掌握所有細節也不用擔心，但現在是時候透過測驗來反思你到目前為止學到的內容了。

這個測驗不計分，所以你可以嘗試任意次數。如果你在某些問題上有困難，請遵循提示並重新檢視材料。在認證考試中，你將再次被測驗這些材料。

### 1. 探索 Hub 並尋找 roberta-large-mnli 檢查點。它執行什麼任務？

<Question
	choices={[
		{
			text: "摘要",
			explain: "再次查看 <a href=\"https://huggingface.co/roberta-large-mnli\">roberta-large-mnli 頁面</a>."
		},
		{
			text: "文本分類",
			explain: "更準確地說，它分類兩個句子是否在三個標籤（矛盾、中性、蘊含）之間邏輯相關——這個任務也稱為自然語言推理</em>.",
			correct: true
		},
		{
			text: "文本生成",
			explain: "再次查看 <a href=\"https://huggingface.co/roberta-large-mnli\">roberta-large-mnli 頁面</a>."
		}
	]}
/>

### 2. 以下程式碼會返回什麼？

```py
from transformers import pipeline

ner = pipeline("ner", grouped_entities=True)
ner("My name is Sylvain and I work at Hugging Face in Brooklyn.")
```

<Question
	choices={[
		{
			text: "它將返回此句子的分類分數，標籤為『正面』或『負面』」這個陳述描述了情感分析功能。這種分析通過將文本分類為情感類別來確定文本中表達的情感色調或態度。",
			explain: "這是不正確的——這應該是 <code>sentiment-analysis</code> 管道"
		},
		{
			text: "它將返回完成此句子的生成文本",
			explain: "這是不正確的——這應該是 <code>text-generation</code> 管道",
		},
		{
			text: "它將返回代表人物、組織或地點的詞彙。",
			explain: "此外，使用 <code>grouped_entities=True<code>，它會將屬於同一實體的詞彙組合在一起，例如『Hugging Face』",
			correct: true
		}
	]}
/>

### 3. 在這個程式碼範例中，應該用什麼來替換「...」？

```py
from transformers import pipeline

filler = pipeline("fill-mask", model="bert-base-cased")
result = filler("...")
```

<Question
	choices={[
		{
			text: "這個 &#60;mask> 一直在等你。",
			explain: "這是錯誤的。請查看 <code>bert-base-cased</code> 模型卡片，並嘗試找出你的錯誤。"
		},
		{
			text: "這個 [MASK] 一直在等你。",
			explain: "這個模型的遮罩標記是 [MASK]。",
			correct: true
		},
		{
			text: "這個男人一直在等你。",
			explain: "這是錯誤的。這個管道用於填充被遮罩的詞彙，所以需要在某處有一個遮罩標記。"
		}
	]}
/>

### 4. 這段程式碼為什麼會失敗？

```py
from transformers import pipeline

classifier = pipeline("zero-shot-classification")
result = classifier("This is a course about the Transformers library")
```

<Question
	choices={[
		{
			text: "此管道需要提供標籤來分類這段文本。",
			explain: "沒錯——正確的程式碼需要包含 <code>candidate_labels=[...]</code>。",
			correct: true
		},
		{
			text: "此管道需要多個句子，而不是只有一個。",
			explain: "這是不正確的，不過當正確使用時，此管道可以接受一個句子列表來處理（就像所有其他管道一樣）。"
		},
		{
			text: "🤗 Transformers 函式庫一如既往地有問題。",
			explain: "我們不會對這個答案給予任何評論！"
		},
		{
			text: "此管道需要更長的輸入；這個輸入太短了。",
			explain: "這是不正確的。請注意，非常長的文本在此管道處理時會被截斷。"
		}
	]}
/>

### 5. What does "transfer learning" mean?

<Question
	choices={[
		{
			text: "將預訓練模型的知識轉移到新模型，並在相同的資料集上訓練它。",
			explain: "不對，那會是同一個模型的兩個版本。"
		},
		{
			text: "通過使用第一個模型的權重來初始化第二個模型，將預訓練模型的知識轉移到新模型。",
			explain: "當第二個模型在新任務上訓練時，它會轉移第一個模型的知識。",
			correct: true
		},
		{
			text: "通過構建與第一個模型相同架構的第二個模型，將預訓練模型的知識轉移到新模型。",
			explain: "架構只是模型構建的方式；在這種情況下沒有知識被分享或轉移。"
		}
	]}
/>

### 6. 對或錯？語言模型在預訓練時通常不需要標籤。

<Question
	choices={[
		{
			text: "對",
			explain: "預訓練通常是<em>自監督的</em>，這意味著標籤會從輸入中自動創建（例如預測下一個詞或填入一些被遮蔽的詞）。",
			correct: true
		},
		{
			text: "錯",
			explain: "這不是正確答案。"
		}
	]}
/>

### 7. 選擇最能描述「模型」、「架構」和「權重」這些術語的句子。

<Question
	choices={[
		{
			text: "如果模型是一棟建築物，那麼它的架構就是藍圖，而權重就是住在裡面的人。",
			explain: "按照這個比喻，權重應該是用來建造建築物的磚塊和其他材料。"
		},
		{
			text: "架構是建構模型的地圖，而其權重是地圖上所表示的城市。",
			explain: "這個比喻的問題在於，地圖通常代表一個既存的現實（法國只有一個名為巴黎的城市）。對於給定的架構，可能有多種權重。"
		},
		{
			text: "架構是建構模型的一系列數學函數，而其權重是這些函數的參數。",
			explain: "相同的數學函數集合（架構）可以通過使用不同的參數（權重）來建構不同的模型。",
			correct: true
		}
	]}
/>

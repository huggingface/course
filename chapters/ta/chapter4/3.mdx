<FrameworkSwitchCourse {fw} />

# முன்பயிற்சி செய்யப்பட்ட மாதிரிகளைப் பகிர்தல்[[sharing-pretrained-models]]

{#if fw === 'pt'}

<CourseFloatingBanner chapter={4}
  classNames="absolute z-10 right-0 top-0"
  notebooks={[
    {label: "Google Colab", value: "https://colab.research.google.com/github/huggingface/notebooks/blob/master/course/en/chapter4/section3_pt.ipynb"},
    {label: "Aws Studio", value: "https://studiolab.sagemaker.aws/import/github/huggingface/notebooks/blob/master/course/en/chapter4/section3_pt.ipynb"},
]} />

{:else}

<CourseFloatingBanner chapter={4}
  classNames="absolute z-10 right-0 top-0"
  notebooks={[
    {label: "Google Colab", value: "https://colab.research.google.com/github/huggingface/notebooks/blob/master/course/en/chapter4/section3_tf.ipynb"},
    {label: "Aws Studio", value: "https://studiolab.sagemaker.aws/import/github/huggingface/notebooks/blob/master/course/en/chapter4/section3_tf.ipynb"},
]} />

{/if}

கீழேயுள்ள படிகளில், முன்பயிற்சி செய்யப்பட்ட மாதிரிகளை 🤗 ஹப்பில் பகிர்வதற்கான எளிதான வழிகளைப் பார்ப்போம். ஹப்பில் நேரடியாக மாதிரிகளைப் பகிர்வதற்கும் புதுப்பிப்பதற்கும் எளிமையான கருவிகள் மற்றும் பயன்பாடுகள் உள்ளன, அவற்றை நாங்கள் கீழே ஆராய்வோம்.

<Youtube id="9yY3RB_GSPM"/>

மாதிரிகளைப் பயிற்றுவிக்கும் அனைத்து பயனர்களும் அவற்றை சமூகத்துடன் பகிர்வதன் மூலம் பங்களிக்க ஊக்குவிக்கிறோம் — மாதிரிகளைப் பகிர்வது, மிகவும் குறிப்பிட்ட தரவுத்தொகுப்புகளில் பயிற்சி பெற்றிருந்தாலும், மற்றவர்களுக்கு உதவும், அவர்களின் நேரத்தையும் கணினி வளங்களையும் மிச்சப்படுத்தும் மற்றும் பயனுள்ள பயிற்சி பெற்ற கலைப்பொருட்களுக்கான அணுகலை வழங்கும். இதையொட்டி, மற்றவர்கள் செய்த வேலையிலிருந்து நீங்கள் பயனடையலாம்!

புதிய மாதிரி களஞ்சியங்களை உருவாக்குவதற்கு மூன்று வழிகள் உள்ளன:

- `push_to_hub` API-ஐப் பயன்படுத்துதல்
- `huggingface_hub` பைதான் நூலகத்தைப் பயன்படுத்துதல்
- வலை இடைமுகத்தைப் பயன்படுத்துதல்

நீங்கள் ஒரு களஞ்சியத்தை உருவாக்கியதும், git மற்றும் git-lfs வழியாக கோப்புகளைப் பதிவேற்றலாம். பின்வரும் பிரிவுகளில் மாதிரி களஞ்சியங்களை உருவாக்குவது மற்றும் వాటికి கோப்புகளைப் பதிவேற்றுவது பற்றி நாங்கள் உங்களுக்கு வழிகாட்டுவோம்.


## `push_to_hub` API-ஐப் பயன்படுத்துதல்[[using-the-pushtohub-api]]

{#if fw === 'pt'}

<Youtube id="Zh0FfmVrKX0"/>

{:else}

<Youtube id="pUh5cGmNV8Y"/>

{/if}

ஹப்பிற்கு கோப்புகளைப் பதிவேற்றுவதற்கான எளிய வழி `push_to_hub` API-ஐப் பயன்படுத்துவதாகும்.

மேலும் செல்வதற்கு முன், நீங்கள் ஒரு அங்கீகார டோக்கனை உருவாக்க வேண்டும், இதன்மூலம் `huggingface_hub` API நீங்கள் யார் என்பதையும், உங்களுக்கு எந்த நேம்ஸ்பேஸ்களில் எழுதும் அணுகல் உள்ளது என்பதையும் அறியும். நீங்கள் `transformers` நிறுவப்பட்ட சூழலில் இருப்பதை உறுதிப்படுத்திக் கொள்ளுங்கள் ([அமைவு](/course/chapter0) பார்க்கவும்). நீங்கள் ஒரு நோட்புக்கில் இருந்தால், உள்நுழைய பின்வரும் செயல்பாட்டைப் பயன்படுத்தலாம்:

```python
from huggingface_hub import notebook_login

notebook_login()
```

ஒரு டெர்மினலில், நீங்கள் இயக்கலாம்:

```bash
huggingface-cli login
```

இரண்டு சந்தர்ப்பங்களிலும், உங்கள் பயனர்பெயர் மற்றும் கடவுச்சொல் கேட்கப்படும், அவை ஹப்பில் உள்நுழைய நீங்கள் பயன்படுத்தும் அதே ஆகும். உங்களிடம் இன்னும் ஹப் சுயவிவரம் இல்லையென்றால், நீங்கள் [இங்கே](https://huggingface.co/join) ஒன்றை உருவாக்க வேண்டும்.

அருமை! இப்போது உங்கள் அங்கீகார டோக்கன் உங்கள் கேச் கோப்புறையில் சேமிக்கப்பட்டுள்ளது. சில களஞ்சியங்களை உருவாக்குவோம்!

{#if fw === 'pt'}

நீங்கள் ஒரு மாதிரியைப் பயிற்றுவிக்க `Trainer` API உடன் விளையாடியிருந்தால், அதை ஹப்பில் பதிவேற்றுவதற்கான எளிதான வழி, உங்கள் `TrainingArguments`-ஐ வரையறுக்கும்போது `push_to_hub=True` என அமைப்பதாகும்:

```py
from transformers import TrainingArguments

training_args = TrainingArguments(
    "bert-finetuned-mrpc", save_strategy="epoch", push_to_hub=True
)
```

நீங்கள் `trainer.train()`-ஐ அழைக்கும்போது, `Trainer` உங்கள் மாதிரியை ஒவ்வொரு முறையும் அது சேமிக்கப்படும்போது (இங்கே ஒவ்வொரு எப்போக்கிலும்) உங்கள் நேம்ஸ்பேஸில் உள்ள ஒரு களஞ்சியத்தில் ஹப்பில் பதிவேற்றும். அந்த களஞ்சியம் நீங்கள் தேர்ந்தெடுத்த வெளியீட்டு கோப்பகத்தைப் போலவே பெயரிடப்படும் (இங்கே `bert-finetuned-mrpc`) ஆனால் நீங்கள் `hub_model_id = "a_different_name"` உடன் வேறு பெயரைத் தேர்வு செய்யலாம்.

உங்கள் மாதிரியை நீங்கள் உறுப்பினராக உள்ள ஒரு நிறுவனத்திற்கு பதிவேற்ற, அதை `hub_model_id = "my_organization/my_repo_name"` உடன் அனுப்பவும்.

உங்கள் பயிற்சி முடிந்ததும், உங்கள் மாதிரியின் கடைசி பதிப்பைப் பதிவேற்ற இறுதி `trainer.push_to_hub()`-ஐ செய்ய வேண்டும். இது பயன்படுத்தப்பட்ட ஹைப்பர்பராமீட்டர்கள் மற்றும் மதிப்பீட்டு முடிவுகளைப் புகாரளிக்கும் அனைத்து தொடர்புடைய மெட்டாடேட்டாவுடன் ஒரு மாதிரி அட்டையையும் உருவாக்கும்! அத்தகைய மாதிரி அட்டையில் நீங்கள் காணக்கூடிய உள்ளடக்கத்தின் ஒரு எடுத்துக்காட்டு இங்கே:

<div class="flex justify-center">
  <img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter4/model_card.png" alt="தானாக உருவாக்கப்பட்ட மாதிரி அட்டையின் ஒரு எடுத்துக்காட்டு." width="100%"/>
</div>

{:else}

உங்கள் மாதிரியைப் பயிற்றுவிக்க நீங்கள் Keras-ஐப் பயன்படுத்துகிறீர்கள் என்றால், அதை ஹப்பில் பதிவேற்றுவதற்கான எளிதான வழி, நீங்கள் `model.fit()`-ஐ அழைக்கும்போது ஒரு `PushToHubCallback`-ஐ அனுப்புவதாகும்:

```py
from transformers import PushToHubCallback

callback = PushToHubCallback(
    "bert-finetuned-mrpc", save_strategy="epoch", tokenizer=tokenizer
)
```

பின்னர் நீங்கள் `model.fit()`-க்கான உங்கள் அழைப்பில் `callbacks=[callback]`-ஐ சேர்க்க வேண்டும். அந்த ಕಾಲ್ಬ୍ୟாக் உங்கள் மாதிரியை ஒவ்வொரு முறையும் அது சேமிக்கப்படும்போது (இங்கே ஒவ்வொரு எப்போக்கிலும்) உங்கள் நேம்ஸ்பேஸில் உள்ள ஒரு களஞ்சியத்தில் ஹப்பில் பதிவேற்றும். அந்த களஞ்சியம் நீங்கள் தேர்ந்தெடுத்த வெளியீட்டு கோப்பகத்தைப் போலவே பெயரிடப்படும் (இங்கே `bert-finetuned-mrpc`) ஆனால் நீங்கள் `hub_model_id = "a_different_name"` உடன் வேறு பெயரைத் தேர்வு செய்யலாம்.

உங்கள் மாதிரியை நீங்கள் உறுப்பினராக உள்ள ஒரு நிறுவனத்திற்கு பதிவேற்ற, அதை `hub_model_id = "my_organization/my_repo_name"` உடன் அனுப்பவும்.

{/if}

ஒரு கீழ் மட்டத்தில், மாடல் ஹப்பை அணுகுவது மாதிரிகள், டோக்கனைசர்கள் மற்றும் உள்ளமைவுப் பொருட்களில் நேரடியாக அவற்றின் `push_to_hub()` முறை வழியாக செய்யப்படலாம். இந்த முறை களஞ்சிய உருவாக்கம் மற்றும் மாதிரி மற்றும் டோக்கனைசர் கோப்புகளை நேரடியாக களஞ்சியத்திற்குத் தள்ளுவது இரண்டையும் கவனித்துக்கொள்கிறது. கீழே நாம் காணும் API போலல்லாமல், கையேடு கையாளுதல் தேவையில்லை.

இது எவ்வாறு செயல்படுகிறது என்பதைப் பற்றிய ஒரு யோசனையைப் பெற, முதலில் ஒரு மாதிரி மற்றும் ஒரு டோக்கனைசரைத் தொடங்குவோம்:

{#if fw === 'pt'}
```py
from transformers import AutoModelForMaskedLM, AutoTokenizer

checkpoint = "camembert-base"

model = AutoModelForMaskedLM.from_pretrained(checkpoint)
tokenizer = AutoTokenizer.from_pretrained(checkpoint)
```
{:else}
```py
from transformers import TFAutoModelForMaskedLM, AutoTokenizer

checkpoint = "camembert-base"

model = TFAutoModelForMaskedLM.from_pretrained(checkpoint)
tokenizer = AutoTokenizer.from_pretrained(checkpoint)
```
{/if}

இவற்றுடன் நீங்கள் விரும்பியதைச் செய்ய நீங்கள் சுதந்திரமாக இருக்கிறீர்கள் — டோக்கனைசரில் டோக்கன்களைச் சேர்க்கவும், மாதிரியைப் பயிற்றுவிக்கவும், அதை நன்றாகச் சரிசெய்யவும். விளைந்த மாதிரி, எடைகள் மற்றும் டோக்கனைசருடன் நீங்கள் மகிழ்ச்சியாக இருக்கும்போது, `model` பொருளில் நேரடியாகக் கிடைக்கும் `push_to_hub()` முறையைப் பயன்படுத்தலாம்:

```py
model.push_to_hub("dummy-model")
```

இது உங்கள் சுயவிவரத்தில் புதிய `dummy-model` களஞ்சியத்தை உருவாக்கும், மேலும் அதை உங்கள் மாதிரி கோப்புகளுடன் நிரப்பும்.
டோக்கனைசருடன் அதையே செய்யுங்கள், இதனால் எல்லா கோப்புகளும் இப்போது இந்த களஞ்சியத்தில் கிடைக்கின்றன:

```py
tokenizer.push_to_hub("dummy-model")
```

நீங்கள் ஒரு நிறுவனத்தைச் சேர்ந்தவராக இருந்தால், அந்த நிறுவனத்தின் நேம்ஸ்பேஸில் பதிவேற்ற `organization` வாதத்தை குறிப்பிடவும்:

```py
tokenizer.push_to_hub("dummy-model", organization="huggingface")
```

நீங்கள் ஒரு குறிப்பிட்ட ஹக்கிங் ஃபேஸ் டோக்கனைப் பயன்படுத்த விரும்பினால், அதை `push_to_hub()` முறைக்கும் குறிப்பிடலாம்:

```py
tokenizer.push_to_hub("dummy-model", organization="huggingface", use_auth_token="<TOKEN>")
```

இப்போது உங்கள் புதிதாக பதிவேற்றப்பட்ட மாதிரியைக் கண்டுபிடிக்க மாடல் ஹப்பிற்குச் செல்லுங்கள்: *https://huggingface.co/user-or-organization/dummy-model*.

"Files and versions" தாவலைக் கிளிக் செய்தால், பின்வரும் ஸ்கிரீன்ஷாட்டில் தெரியும் கோப்புகளை நீங்கள் காண வேண்டும்:

{#if fw === 'pt'}
<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter4/push_to_hub_dummy_model.png" alt="டோக்கனைசர் மற்றும் மாதிரி கோப்புகள் இரண்டையும் கொண்ட போலி மாதிரி." width="80%"/>
</div>
{:else}
<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter4/push_to_hub_dummy_model_tf.png" alt="டோக்கனைசர் மற்றும் மாதிரி கோப்புகள் இரண்டையும் கொண்ட போலி மாதிரி." width="80%"/>
</div>
{/if}

<Tip>

✏️ **முயற்சி செய்து பாருங்கள்!** `bert-base-cased` சோதனைச் சாவடியுடன் தொடர்புடைய மாதிரி மற்றும் டோக்கனைசரை எடுத்து, `push_to_hub()` முறையைப் பயன்படுத்தி உங்கள் நேம்ஸ்பேஸில் உள்ள ஒரு ரெப்போவிற்கு பதிவேற்றவும். அதை நீக்குவதற்கு முன், ரெப்போ உங்கள் பக்கத்தில் சரியாகத் தோன்றுகிறதா என்பதை இருமுறை சரிபார்க்கவும்.

</Tip>

நீங்கள் பார்த்தபடி, `push_to_hub()` முறை பல வாதங்களை ஏற்றுக்கொள்கிறது, இது ஒரு குறிப்பிட்ட களஞ்சியம் அல்லது நிறுவன நேம்ஸ்பேஸில் பதிவேற்றுவதை அல்லது வேறு API டோக்கனைப் பயன்படுத்துவதை சாத்தியமாக்குகிறது. என்ன சாத்தியம் என்பதைப் பற்றிய ஒரு யோசனையைப் பெற, [🤗 Transformers ஆவணப்படுத்தலில்](https://huggingface.co/transformers/model_sharing) நேரடியாகக் கிடைக்கும் முறை விவரக்குறிப்பைப் பார்க்க பரிந்துரைக்கிறோம்.

`push_to_hub()` முறை [`huggingface_hub`](https://github.com/huggingface/huggingface_hub) பைதான் தொகுப்பால் ஆதரிக்கப்படுகிறது, இது ஹக்கிங் ஃபேஸ் ஹப்பிற்கு நேரடி API-ஐ வழங்குகிறது. இது 🤗 Transformers மற்றும் [`allenlp`](https://github.com/allenai/allennlp) போன்ற பல இயந்திர கற்றல் நூலகங்களில் ஒருங்கிணைக்கப்பட்டுள்ளது. இந்த அத்தியாயத்தில் நாங்கள் 🤗 Transformers ஒருங்கிணைப்பில் கவனம் செலுத்தினாலும், அதை உங்கள் சொந்த குறியீடு அல்லது நூலகத்தில் ஒருங்கிணைப்பது எளிது.

உங்கள் புதிதாக உருவாக்கப்பட்ட களஞ்சியத்திற்கு கோப்புகளை எவ்வாறு பதிவேற்றுவது என்பதைப் பார்க்க கடைசி பகுதிக்குச் செல்லுங்கள்!

## `huggingface_hub` பைதான் நூலகத்தைப் பயன்படுத்துதல்[[using-the-huggingfacehub-python-library]]

`huggingface_hub` பைதான் நூலகம் என்பது மாதிரி மற்றும் தரவுத்தொகுப்பு மையங்களுக்கான கருவிகளின் தொகுப்பை வழங்கும் ஒரு தொகுப்பாகும். இது ஹப்பில் உள்ள களஞ்சியங்களைப் பற்றிய தகவல்களைப் பெறுவது மற்றும் அவற்றை நிர்வகிப்பது போன்ற பொதுவான பணிகளுக்கு எளிய முறைகள் மற்றும் வகுப்புகளை வழங்குகிறது. இது அந்த களஞ்சியங்களின் உள்ளடக்கத்தை நிர்வகிக்க மற்றும் உங்கள் திட்டங்கள் மற்றும் நூலகங்களில் ஹப்பை ஒருங்கிணைக்க git-இன் மேல் செயல்படும் எளிய API-களை வழங்குகிறது.

`push_to_hub` API-ஐப் பயன்படுத்துவதைப் போலவே, இதற்கும் உங்கள் API டோக்கனை உங்கள் கேச்சில் சேமிக்க வேண்டும். இதைச் செய்ய, முந்தைய பிரிவில் குறிப்பிட்டபடி, CLI-இலிருந்து `login` கட்டளையைப் பயன்படுத்த வேண்டும் (மீண்டும், கூகிள் கோலாப்பில் இயங்கினால் இந்த கட்டளைகளுக்கு முன்னால் `!` எழுத்தைச் சேர்க்க மறக்காதீர்கள்):

```bash
huggingface-cli login
```

`huggingface_hub` தொகுப்பு நமது நோக்கத்திற்கு பயனுள்ள பல முறைகள் மற்றும் வகுப்புகளை வழங்குகிறது. முதலாவதாக, களஞ்சிய உருவாக்கம், நீக்குதல் மற்றும் பிறவற்றை நிர்வகிக்க சில முறைகள் உள்ளன:

```python no-format
from huggingface_hub import (
    # User management
    login,
    logout,
    whoami,

    # Repository creation and management
    create_repo,
    delete_repo,
    update_repo_visibility,

    # And some methods to retrieve/change information about the content
    list_models,
    list_datasets,
    list_metrics,
    list_repo_files,
    upload_file,
    delete_file,
)
```


கூடுதலாக, இது ஒரு உள்ளூர் களஞ்சியத்தை நிர்வகிக்க மிகவும் சக்திவாய்ந்த `Repository` வகுப்பை வழங்குகிறது. இந்த முறைகள் மற்றும் அந்த வகுப்பை அடுத்த சில பிரிவுகளில் அவற்றை எவ்வாறு பயன்படுத்துவது என்பதைப் புரிந்துகொள்ள ஆராய்வோம்.

`create_repo` முறையை ஹப்பில் ஒரு புதிய களஞ்சியத்தை உருவாக்க பயன்படுத்தலாம்:

```py
from huggingface_hub import create_repo

create_repo("dummy-model")
```

இது உங்கள் நேம்ஸ்பேஸில் `dummy-model` களஞ்சியத்தை உருவாக்கும். நீங்கள் விரும்பினால், `organization` வாதத்தைப் பயன்படுத்தி களஞ்சியம் எந்த நிறுவனத்தைச் சேர்ந்தது என்பதைக் குறிப்பிடலாம்:

```py
from huggingface_hub import create_repo

create_repo("dummy-model", organization="huggingface")
```

இது `huggingface` நேம்ஸ்பேஸில் `dummy-model` களஞ்சியத்தை உருவாக்கும், நீங்கள் அந்த நிறுவனத்தைச் சேர்ந்தவர் என்று ধরেக்கொள்கிறோம்.
பயனுள்ளதாக இருக்கும் பிற வாதங்கள்:

- `private`, களஞ்சியம் மற்றவர்களிடமிருந்து தெரிய வேண்டுமா இல்லையா என்பதைக் குறிப்பிட.
- `token`, உங்கள் கேச்சில் சேமிக்கப்பட்ட டோக்கனை ஒரு குறிப்பிட்ட டோக்கன் மூலம் மேலெழுத விரும்பினால்.
- `repo_type`, நீங்கள் ஒரு மாதிரிக்கு பதிலாக ஒரு `dataset` அல்லது `space`-ஐ உருவாக்க விரும்பினால். ஏற்றுக்கொள்ளப்பட்ட மதிப்புகள் `"dataset"` மற்றும் `"space"`.

களஞ்சியம் உருவாக்கப்பட்டதும், நாம் ಅದಕ್ಕೆ கோப்புகளைச் சேர்க்க வேண்டும்! இதை கையாளக்கூடிய மூன்று வழிகளைக் காண அடுத்த பகுதிக்குச் செல்லுங்கள்.


## வலை இடைமுகத்தைப் பயன்படுத்துதல்[[using-the-web-interface]]

வலை இடைமுகம் நேரடியாக ஹப்பில் களஞ்சியங்களை நிர்வகிக்க கருவிகளை வழங்குகிறது. இடைமுகத்தைப் பயன்படுத்தி, நீங்கள் எளிதாக களஞ்சியங்களை உருவாக்கலாம், கோப்புகளைச் சேர்க்கலாம் (பெரியவை கூட!), மாதிரிகளை ஆராயலாம், வேறுபாடுகளைக் காட்சிப்படுத்தலாம் மற்றும் பல.

ஒரு புதிய களஞ்சியத்தை உருவாக்க, [huggingface.co/new](https://huggingface.co/new)-ஐப் பார்வையிடவும்:

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter4/new_model.png" alt="ஒரு புதிய மாதிரி களஞ்சியத்தை உருவாக்குவதற்குப் பயன்படுத்தப்படும் மாதிரியைக் காட்டும் பக்கம்." width="80%"/>
</div>

முதலில், களஞ்சியத்தின் உரிமையாளரைக் குறிப்பிடவும்: இது நீங்களாகவோ அல்லது நீங்கள் இணைந்திருக்கும் எந்தவொரு நிறுவனமாகவோ இருக்கலாம். நீங்கள் ஒரு நிறுவனத்தைத் தேர்வுசெய்தால், மாதிரி நிறுவனத்தின் பக்கத்தில் இடம்பெறும் மற்றும் நிறுவனத்தின் ஒவ்வொரு உறுப்பினரும் களஞ்சியத்திற்கு பங்களிக்கும் திறனைக் கொண்டிருப்பார்கள்.

அடுத்து, உங்கள் மாதிரியின் பெயரை உள்ளிடவும். இது களஞ்சியத்தின் பெயராகவும் இருக்கும். இறுதியாக, உங்கள் மாதிரி பொதுவில் இருக்க வேண்டுமா அல்லது தனிப்பட்டதாக இருக்க வேண்டுமா என்பதைக் குறிப்பிடலாம். தனிப்பட்ட மாதிரிகள் பொதுப் பார்வையிலிருந்து மறைக்கப்பட்டுள்ளன.

உங்கள் மாதிரி களஞ்சியத்தை உருவாக்கிய பிறகு, நீங்கள் இது போன்ற ஒரு பக்கத்தைக் காண வேண்டும்:

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter4/empty_model.png" alt="ஒரு புதிய களஞ்சியத்தை உருவாக்கிய பிறகு ஒரு வெற்று மாதிரி பக்கம்." width="80%"/>
</div>

இங்குதான் உங்கள் மாதிரி ஹோஸ்ட் செய்யப்படும். அதை நிரப்பத் தொடங்க, நீங்கள் வலை இடைமுகத்திலிருந்து நேரடியாக ஒரு README கோப்பைச் சேர்க்கலாம்.

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter4/dummy_model.png" alt="Markdown திறன்களைக் காட்டும் README கோப்பு." width="80%"/>
</div>

README கோப்பு Markdown-இல் உள்ளது — அதனுடன் காட்டுத்தனமாக செல்ல தயங்காதீர்கள்! இந்த அத்தியாயத்தின் மூன்றாவது பகுதி ஒரு மாதிரி அட்டையை உருவாக்குவதற்கு அர்ப்பணிக்கப்பட்டுள்ளது. உங்கள் மாதிரிக்கு மதிப்பைக் கொண்டுவருவதில் இவை முதன்மையான முக்கியத்துவம் வாய்ந்தவை, ஏனெனில் அவை மற்றவர்களுக்கு அது என்ன செய்ய முடியும் என்பதை நீங்கள் சொல்லும் இடம்.

நீங்கள் "Files and versions" தாவலைப் பார்த்தால், இன்னும் பல கோப்புகள் இல்லை என்பதை நீங்கள் காண்பீர்கள் — நீங்கள் இப்போது உருவாக்கிய *README.md* மற்றும் பெரிய கோப்புகளைக் கண்காணிக்கும் *.gitattributes* கோப்பு மட்டுமே.

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter4/files.png" alt="'Files and versions' தாவல் .gitattributes மற்றும் README.md கோப்புகளை மட்டுமே காட்டுகிறது." width="80%"/>
</div>

அடுத்து சில புதிய கோப்புகளை எவ்வாறு சேர்ப்பது என்று பார்ப்போம்.

## மாதிரி கோப்புகளைப் பதிவேற்றுதல்[[uploading-the-model-files]]

ஹக்கிங் ஃபேஸ் ஹப்பில் கோப்புகளை நிர்வகிக்கும் அமைப்பு வழக்கமான கோப்புகளுக்கு git-ஐ அடிப்படையாகக் கொண்டது, மற்றும் பெரிய கோப்புகளுக்கு git-lfs (இது [Git Large File Storage](https://git-lfs.github.com/)-க்கு நிற்கிறது).

அடுத்த பகுதியில், ஹப்பிற்கு கோப்புகளைப் பதிவேற்றுவதற்கான மூன்று வெவ்வேறு வழிகளைப் பார்க்கிறோம்: `huggingface_hub` வழியாக மற்றும் git கட்டளைகள் வழியாக.

### `upload_file` அணுகுமுறை[[the-uploadfile-approach]]

`upload_file`-ஐப் பயன்படுத்துவதற்கு உங்கள் கணினியில் git மற்றும் git-lfs நிறுவப்பட்டிருக்க வேண்டியதில்லை. இது HTTP POST கோரிக்கைகளைப் பயன்படுத்தி கோப்புகளை நேரடியாக 🤗 ஹப்பிற்குத் தள்ளுகிறது. இந்த அணுகுமுறையின் ஒரு வரம்பு என்னவென்றால், இது 5GB-ஐ விட பெரிய கோப்புகளைக் கையாளாது.
உங்கள் கோப்புகள் 5GB-ஐ விட பெரியதாக இருந்தால், கீழே விவரிக்கப்பட்டுள்ள மற்ற இரண்டு முறைகளைப் பின்பற்றவும்.

API-ஐ பின்வருமாறு பயன்படுத்தலாம்:

```py
from huggingface_hub import upload_file

upload_file(
    "<path_to_file>/config.json",
    path_in_repo="config.json",
    repo_id="<namespace>/dummy-model",
)
```

இது `<path_to_file>`-இல் கிடைக்கும் `config.json` கோப்பை `dummy-model` களஞ்சியத்திற்கு, களஞ்சியத்தின் ரூட்டில் `config.json` ஆக பதிவேற்றும்.
பயனுள்ளதாக இருக்கும் பிற வாதங்கள்:

- `token`, உங்கள் கேச்சில் சேமிக்கப்பட்ட டோக்கனை ஒரு குறிப்பிட்ட டோக்கன் மூலம் மேலெழுத விரும்பினால்.
- `repo_type`, நீங்கள் ஒரு மாதிரிக்கு பதிலாக ஒரு `dataset` அல்லது `space`-க்கு பதிவேற்ற விரும்பினால். ஏற்றுக்கொள்ளப்பட்ட மதிப்புகள் `"dataset"` மற்றும் `"space"`.


### `Repository` வகுப்பு[[the-repository-class]]

`Repository` வகுப்பு ஒரு உள்ளூர் களஞ்சியத்தை git-போன்ற முறையில் நிர்வகிக்கிறது. இது நமக்குத் தேவையான அனைத்து அம்சங்களையும் வழங்க git-உடன் ஏற்படக்கூடிய பெரும்பாலான வலி புள்ளிகளை சுருக்குகிறது.

இந்த வகுப்பைப் பயன்படுத்த git மற்றும் git-lfs நிறுவப்பட்டிருக்க வேண்டும், எனவே நீங்கள் தொடங்குவதற்கு முன் git-lfs நிறுவப்பட்டுள்ளதா என்பதை உறுதிப்படுத்திக் கொள்ளுங்கள் ([நிறுவல் வழிமுறைகளுக்கு இங்கே](https://git-lfs.github.com/) பார்க்கவும்).

நாம் இப்போது உருவாக்கிய களஞ்சியத்துடன் விளையாடத் தொடங்க, தொலைநிலை களஞ்சியத்தை குளோனிங் செய்வதன் மூலம் அதை ஒரு உள்ளூர் கோப்புறையில் தொடங்குவதன் மூலம் தொடங்கலாம்:

```py
from huggingface_hub import Repository

repo = Repository("<path_to_dummy_folder>", clone_from="<namespace>/dummy-model")
```

இது நமது வேலை செய்யும் கோப்பகத்தில் `<path_to_dummy_folder>` கோப்புறையை உருவாக்கியது. இந்த கோப்புறையில் `.gitattributes` கோப்பு மட்டுமே உள்ளது, ஏனெனில் `create_repo` மூலம் களஞ்சியத்தை உருவாக்கும்போது உருவாக்கப்பட்ட ஒரே கோப்பு அதுதான்.

இந்த கட்டத்தில் இருந்து, நாம் பல பாரம்பரிய git முறைகளைப் பயன்படுத்தலாம்:

```py
repo.git_pull()
repo.git_add()
repo.git_commit()
repo.git_push()
repo.git_tag()
```

மற்றும் பிற! கிடைக்கக்கூடிய அனைத்து முறைகளின் மேலோட்டத்திற்கு [இங்கே](https://github.com/huggingface/huggingface_hub/tree/main/src/huggingface_hub#advanced-programmatic-repository-management) கிடைக்கும் `Repository` ஆவணத்தைப் பார்க்க பரிந்துரைக்கிறோம்.

தற்போது, நம்மிடம் ஒரு மாதிரி மற்றும் ஒரு டோக்கனைசர் உள்ளது, அதை நாம் ஹப்பிற்குத் தள்ள விரும்புகிறோம். நாம் களஞ்சியத்தை வெற்றிகரமாக குளோன் செய்துள்ளோம், எனவே அந்த களஞ்சியத்திற்குள் கோப்புகளைச் சேமிக்கலாம்.

சமீபத்திய மாற்றங்களைப் பெறுவதன் மூலம் நமது உள்ளூர் குளோன் புதுப்பித்த நிலையில் இருப்பதை முதலில் உறுதிசெய்கிறோம்:

```py
repo.git_pull()
```

அது முடிந்ததும், மாதிரி மற்றும் டோக்கனைசர் கோப்புகளைச் சேமிக்கிறோம்:

```py
model.save_pretrained("<path_to_dummy_folder>")
tokenizer.save_pretrained("<path_to_dummy_folder>")
```

`<path_to_dummy_folder>` இப்போது அனைத்து மாதிரி மற்றும் டோக்கனைசர் கோப்புகளையும் கொண்டுள்ளது. கோப்புகளை ஸ்டேஜிங் பகுதிக்குச் சேர்ப்பது, அவற்றை கமிட் செய்வது மற்றும் அவற்றை ஹப்பிற்குத் தள்ளுவது போன்ற வழக்கமான git பணிப்பாய்வுகளைப் பின்பற்றுகிறோம்:

```py
repo.git_add()
repo.git_commit("Add model and tokenizer files")
repo.git_push()
```

வாழ்த்துக்கள்! நீங்கள் இப்போது உங்கள் முதல் கோப்புகளை ஹப்பில் தள்ளியுள்ளீர்கள்.

### git-அடிப்படையிலான அணுகுமுறை[[the-git-based-approach]]

இது கோப்புகளைப் பதிவேற்றுவதற்கான மிகவும் அடிப்படையான அணுகுமுறை: நாங்கள் அதை git மற்றும் git-lfs உடன் நேரடியாகச் செய்வோம். பெரும்பாலான சிரமங்கள் முந்தைய அணுகுமுறைகளால் சுருக்கப்பட்டுள்ளன, ஆனால் பின்வரும் முறையில் சில எச்சரிக்கைகள் உள்ளன, எனவே நாங்கள் ஒரு சிக்கலான பயன்பாட்டு வழக்கத்தைப் பின்பற்றுவோம்.

இந்த வகுப்பைப் பயன்படுத்த git மற்றும் git-lfs நிறுவப்பட்டிருக்க வேண்டும், எனவே நீங்கள் தொடங்குவதற்கு முன் [git-lfs](https://git-lfs.github.com/) நிறுவப்பட்டுள்ளதா என்பதை உறுதிப்படுத்திக் கொள்ளுங்கள் (நிறுவல் வழிமுறைகளுக்கு இங்கே பார்க்கவும்).

முதலில் git-lfs-ஐத் தொடங்குவதன் மூலம் தொடங்கவும்:

```bash
git lfs install
```

```bash
Updated git hooks.
Git LFS initialized.
```

அது முடிந்ததும், முதல் படி உங்கள் மாதிரி களஞ்சியத்தை குளோன் செய்வதாகும்:

```bash
git clone https://huggingface.co/<namespace>/<your-model-id>
```

எனது பயனர்பெயர் `lysandre` மற்றும் நான் `dummy` என்ற மாதிரி பெயரைப் பயன்படுத்தியுள்ளேன், எனவே எனக்கு கட்டளை பின்வருமாறு தெரிகிறது:

```
git clone https://huggingface.co/lysandre/dummy
```

எனது வேலை செய்யும் கோப்பகத்தில் இப்போது *dummy* என்ற கோப்புறை உள்ளது. நான் கோப்புறையில் `cd` செய்து உள்ளடக்கங்களைப் பார்க்கலாம்:

```bash
cd dummy && ls
```

```bash
README.md
```

If you just created your repository using Hugging Face Hub's `create_repo` method, this folder should only contain a hidden `.gitattributes` file. If you followed the instructions in the previous section to create a repository using the web interface, the folder should contain a single *README.md* file alongside the hidden `.gitattributes` file, as shown here.

Adding a regular-sized file, such as a configuration file, a vocabulary file, or basically any file under a few megabytes, is done exactly as one would do it in any git-based system. However, bigger files must be registered through git-lfs in order to push them to *huggingface.co*. 

Let's go back to Python for a bit to generate a model and tokenizer that we'd like to commit to our dummy repository:

{#if fw === 'pt'}
```py
from transformers import AutoModelForMaskedLM, AutoTokenizer

checkpoint = "camembert-base"

model = AutoModelForMaskedLM.from_pretrained(checkpoint)
tokenizer = AutoTokenizer.from_pretrained(checkpoint)

# Do whatever with the model, train it, fine-tune it...

model.save_pretrained("<path_to_dummy_folder>")
tokenizer.save_pretrained("<path_to_dummy_folder>")
```
{:else}
```py
from transformers import TFAutoModelForMaskedLM, AutoTokenizer

checkpoint = "camembert-base"

model = TFAutoModelForMaskedLM.from_pretrained(checkpoint)
tokenizer = AutoTokenizer.from_pretrained(checkpoint)

# Do whatever with the model, train it, fine-tune it...

model.save_pretrained("<path_to_dummy_folder>")
tokenizer.save_pretrained("<path_to_dummy_folder>")
```
{/if}

Now that we've saved some model and tokenizer artifacts, let's take another look at the *dummy* folder:

```bash
ls
```

{#if fw === 'pt'}
```bash
config.json  pytorch_model.bin  README.md  sentencepiece.bpe.model  special_tokens_map.json tokenizer_config.json  tokenizer.json
```

If you look at the file sizes (for example, with `ls -lh`), you should see that the model state dict file (*pytorch_model.bin*) is the only outlier, at more than 400 MB.

{:else}
```bash
config.json  README.md  sentencepiece.bpe.model  special_tokens_map.json  tf_model.h5  tokenizer_config.json  tokenizer.json
```

If you look at the file sizes (for example, with `ls -lh`), you should see that the model state dict file (*t5_model.h5*) is the only outlier, at more than 400 MB.

{/if}

<Tip>
✏️ When creating the repository from the web interface, the *.gitattributes* file is automatically set up to consider files with certain extensions, such as *.bin* and *.h5*, as large files, and git-lfs will track them with no necessary setup on your side.
</Tip> 

We can now go ahead and proceed like we would usually do with traditional Git repositories. We can add all the files to Git's staging environment using the `git add` command:

```bash
git add .
```

We can then have a look at the files that are currently staged:

```bash
git status
```

{#if fw === 'pt'}
```bash
On branch main
Your branch is up to date with 'origin/main'.

Changes to be committed:
  (use "git restore --staged <file>..." to unstage)
  modified:   .gitattributes
	new file:   config.json
	new file:   pytorch_model.bin
	new file:   sentencepiece.bpe.model
	new file:   special_tokens_map.json
	new file:   tokenizer.json
	new file:   tokenizer_config.json
```
{:else}
```bash
On branch main
Your branch is up to date with 'origin/main'.

Changes to be committed:
  (use "git restore --staged <file>..." to unstage)
  modified:   .gitattributes
  	new file:   config.json
	new file:   sentencepiece.bpe.model
	new file:   special_tokens_map.json
	new file:   tf_model.h5
	new file:   tokenizer.json
	new file:   tokenizer_config.json
```
{/if}

Similarly, we can make sure that git-lfs is tracking the correct files by using its `status` command:

```bash
git lfs status
```

{#if fw === 'pt'}
```bash
On branch main
Objects to be pushed to origin/main:


Objects to be committed:

	config.json (Git: bc20ff2)
	pytorch_model.bin (LFS: 35686c2)
	sentencepiece.bpe.model (LFS: 988bc5a)
	special_tokens_map.json (Git: cb23931)
	tokenizer.json (Git: 851ff3e)
	tokenizer_config.json (Git: f0f7783)

Objects not staged for commit:


```

We can see that all files have `Git` as a handler, except *pytorch_model.bin* and *sentencepiece.bpe.model*, which have `LFS`. Great!

{:else}
```bash
On branch main
Objects to be pushed to origin/main:


Objects to be committed:

	config.json (Git: bc20ff2)
	sentencepiece.bpe.model (LFS: 988bc5a)
	special_tokens_map.json (Git: cb23931)
	tf_model.h5 (LFS: 86fce29)
	tokenizer.json (Git: 851ff3e)
	tokenizer_config.json (Git: f0f7783)

Objects not staged for commit:


```

We can see that all files have `Git` as a handler, except *t5_model.h5*, which has `LFS`. Great!

{/if}

Let's proceed to the final steps, committing and pushing to the *huggingface.co* remote repository:

```bash
git commit -m "First model version"
```

{#if fw === 'pt'}
```bash
[main b08aab1] First model version
 7 files changed, 29027 insertions(+)
  6 files changed, 36 insertions(+)
 create mode 100644 config.json
 create mode 100644 pytorch_model.bin
 create mode 100644 sentencepiece.bpe.model
 create mode 100644 special_tokens_map.json
 create mode 100644 tokenizer.json
 create mode 100644 tokenizer_config.json
```
{:else}
```bash
[main b08aab1] First model version
 6 files changed, 36 insertions(+)
 create mode 100644 config.json
 create mode 100644 sentencepiece.bpe.model
 create mode 100644 special_tokens_map.json
 create mode 100644 tf_model.h5
 create mode 100644 tokenizer.json
 create mode 100644 tokenizer_config.json
```
{/if}

Pushing can take a bit of time, depending on the speed of your internet connection and the size of your files:

```bash
git push
```

```bash
Uploading LFS objects: 100% (1/1), 433 MB | 1.3 MB/s, done.
Enumerating objects: 11, done.
Counting objects: 100% (11/11), done.
Delta compression using up to 12 threads
Compressing objects: 100% (9/9), done.
Writing objects: 100% (9/9), 288.27 KiB | 6.27 MiB/s, done.
Total 9 (delta 1), reused 0 (delta 0), pack-reused 0
To https://huggingface.co/lysandre/dummy
   891b41d..b08aab1  main -> main
```

{#if fw === 'pt'}
If we take a look at the model repository when this is finished, we can see all the recently added files:

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter4/full_model.png" alt="The 'Files and versions' tab now contains all the recently uploaded files." width="80%"/>
</div>

The UI allows you to explore the model files and commits and to see the diff introduced by each commit:

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter4/diffs.gif" alt="The diff introduced by the recent commit." width="80%"/>
</div>
{:else}
If we take a look at the model repository when this is finished, we can see all the recently added files:

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter4/full_model_tf.png" alt="The 'Files and versions' tab now contains all the recently uploaded files." width="80%"/>
</div>

The UI allows you to explore the model files and commits and to see the diff introduced by each commit:

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter4/diffstf.gif" alt="The diff introduced by the recent commit." width="80%"/>
</div>
{/if}

<!-- DISABLE-FRONTMATTER-SECTIONS -->

# Kuis Akhir Bab[[end-of-chapter-quiz]]

<CourseFloatingBanner
    chapter={5}
    classNames="absolute z-10 right-0 top-0"
/>

Bab ini mencakup banyak hal! Jangan khawatir jika Anda belum memahami semua detailnya; bab-bab berikutnya akan membantu Anda memahami bagaimana semuanya bekerja di balik layar.

Namun sebelum melanjutkan, mari kita uji apa yang sudah Anda pelajari di bab ini.

### 1. Fungsi `load_dataset()` di 🤗 Datasets memungkinkan Anda memuat dataset dari lokasi mana saja berikut ini?

<Question
	choices={[
		{
			text: "Secara lokal, misalnya di laptop Anda",
			explain: "Benar! Anda dapat memberikan path ke file lokal melalui argumen <code>data_files</code> pada <code>load_dataset()</code> untuk memuat dataset lokal.",
			correct: true
		},
		{
			text: "Dari Hugging Face Hub",
			explain: "Benar! Anda bisa memuat dataset dari Hub dengan memberikan ID dataset, misalnya <code>load_dataset('emotion')</code>.",
			correct: true
		},
		{
			text: "Dari server jarak jauh",
			explain: "Benar! Anda bisa memberikan URL ke argumen <code>data_files</code> pada <code>load_dataset()</code> untuk memuat file dari server jarak jauh.",
			correct: true
		},
	]}
/>

### 2. Misalkan Anda memuat salah satu tugas GLUE sebagai berikut:

```py
from datasets import load_dataset

dataset = load_dataset("glue", "mrpc", split="train")
```

Perintah mana yang akan menghasilkan sampel acak sebanyak 50 elemen dari `dataset`?

<Question
	choices={[
		{
			text: "<code>dataset.sample(50)</code>",
			explain: "Ini salah -- tidak ada metode <code>Dataset.sample()</code>."
		},
		{
			text: "<code>dataset.shuffle().select(range(50))</code>",
			explain: "Benar! Seperti yang Anda lihat di bab ini, Anda harus mengacak dataset terlebih dahulu dan kemudian memilih sampel darinya.",
			correct: true
		},
		{
			text: "<code>dataset.select(range(50)).shuffle()</code>",
			explain: "Ini salah -- meskipun kodenya akan berjalan, itu hanya akan mengacak 50 elemen pertama dari dataset."
		}
	]}
/>

### 3. Misalkan Anda memiliki dataset tentang hewan peliharaan bernama `pets_dataset`, yang memiliki kolom `name` yang menunjukkan nama setiap hewan. Pendekatan mana yang akan memungkinkan Anda menyaring dataset untuk semua hewan peliharaan yang namanya dimulai dengan huruf "L"?

<Question
	choices={[
		{
			text: "<code>pets_dataset.filter(lambda x : x['name'].startswith('L'))</code>",
			explain: "Benar! Menggunakan fungsi lambda Python untuk filter cepat seperti ini adalah ide bagus. Bisakah Anda pikirkan solusi lain?",
			correct: true
		},
		{
			text: "<code>pets_dataset.filter(lambda x['name'].startswith('L'))</code>",
			explain: "Ini salah -- fungsi lambda harus dalam bentuk umum <code>lambda *argumen* : *ekspresi*</code>, jadi Anda perlu memberikan argumen di sini."
		},
		{
			text: "Buat fungsi seperti <code>def filter_names(x): return x['name'].startswith('L')</code> lalu jalankan <code>pets_dataset.filter(filter_names)</code>.",
			explain: "Benar! Seperti pada <code>Dataset.map()</code>, Anda bisa memberikan fungsi eksplisit ke <code>Dataset.filter()</code>. Ini berguna untuk logika kompleks yang tidak cocok dalam fungsi lambda pendek.",
			correct: true
		}
	]}
/>

### 4. Apa itu memory mapping?

<Question
	choices={[
		{
			text: "Pemetaan antara RAM CPU dan GPU",
			explain: "Salah -- coba lagi!",
		},
		{
			text: "Pemetaan antara RAM dan penyimpanan di filesystem",
			explain: "Benar! 🤗 Datasets memperlakukan setiap dataset sebagai file memory-mapped. Ini memungkinkan pustaka mengakses dan memproses elemen dataset tanpa harus memuat semuanya ke dalam memori.",
			correct: true
		},
		{
			text: "Pemetaan antara dua file di cache 🤗 Datasets",
			explain: "Ini salah -- coba lagi!"
		}
	]}
/>

### 5. Apa saja manfaat utama dari memory mapping?

<Question
	choices={[
		{
			text: "Mengakses file memory-mapped lebih cepat daripada membaca atau menulis ke disk.",
			explain: "Benar! Ini memungkinkan 🤗 Datasets bekerja dengan sangat cepat. Tapi ini bukan satu-satunya manfaat.",
			correct: true
		},
		{
			text: "Aplikasi dapat mengakses bagian data dari file yang sangat besar tanpa harus memuat seluruh file ke RAM terlebih dahulu.",
			explain: "Benar! Ini memungkinkan 🤗 Datasets memuat dataset multi-gigabyte di laptop Anda tanpa membebani CPU.",
			correct: true
		},
		{
			text: "Mengonsumsi lebih sedikit energi, sehingga baterai Anda lebih tahan lama.",
			explain: "Ini salah -- coba lagi!"
		}
	]}
/>

### 6. Mengapa kode berikut gagal?

```py
from datasets import load_dataset

dataset = load_dataset("allocine", streaming=True, split="train")
dataset[0]
```

<Question
	choices={[
		{
			text: "Ia mencoba untuk melakukan streaming dataset yang terlalu besar untuk dimuat ke dalam RAM.",
			explain: "Ini salah -- dataset streaming didekompresi saat dijalankan, dan Anda dapat memproses dataset berukuran terabyte dengan RAM yang sangat sedikit!",
		},
		{
			text: "Ia mencoba mengakses <code>IterableDataset</code>.",
			explain: "Benar! <code>IterableDataset</code> adalah generator, bukan kontainer, jadi Anda harus mengakses elemennya menggunakan <code>next(iter(dataset))</code>.",
			correct: true
		},
		{
			text: "Dataset <code>allocine</code> tidak memiliki split <code>train</code>.",
			explain: "Ini salah -- lihat [kartu dataset <code>allocine</code>](https://huggingface.co/datasets/allocine) di Hub untuk melihat split yang tersedia."
		}
	]}
/>

### 7. Apa saja manfaat utama dari membuat kartu dataset?

<Question
	choices={[
		{
			text: "Memberikan informasi tentang tujuan penggunaan dan tugas yang didukung oleh dataset agar komunitas dapat membuat keputusan penggunaan yang tepat.",
			explain: "Benar! Dataset yang tidak terdokumentasi bisa digunakan untuk melatih model yang tidak mencerminkan niat pembuat dataset, atau melanggar privasi dan hak cipta.",
			correct : true
		},
		{
			text: "Membantu menyoroti bias yang ada dalam korpus.",
			explain: "Benar! Hampir semua dataset memiliki bias, yang dapat berdampak negatif di tahap lanjut. Menyadarinya membantu pembuat model menangani bias tersebut.",
			correct : true
		},
		{
			text: "Meningkatkan kemungkinan dataset saya digunakan oleh komunitas.",
			explain: "Benar! Kartu dataset yang ditulis dengan baik cenderung membuat dataset Anda lebih menarik bagi pengguna lain.",
			correct: true
		},
	]}
/>

### 8. Apa itu pencarian semantik (semantic search)?

<Question
	choices={[
		{
			text: "Cara untuk mencari kecocokan kata secara persis antara kueri dan dokumen dalam korpus",
			explain: "Ini salah -- pencarian semacam ini disebut *pencarian leksikal*, seperti yang biasa Anda lihat di mesin pencari tradisional."
		},
		{
			text: "Cara mencari dokumen yang cocok dengan memahami makna kontekstual dari kueri",
			explain: "Benar! Pencarian semantik menggunakan vektor embedding untuk merepresentasikan kueri dan dokumen, lalu menggunakan metrik kemiripan untuk mengukurnya.",
			correct: true
		},
		{
			text: "Cara meningkatkan akurasi pencarian",
			explain: "Benar! Pencarian semantik dapat menangkap maksud kueri lebih baik daripada pencocokan kata kunci, dan biasanya menghasilkan dokumen dengan presisi lebih tinggi.",
			correct: true
		}
	]}
/>

### 9. Dalam pencarian semantik asimetris, Anda biasanya memiliki:

<Question
	choices={[
		{
			text: "Kueri pendek dan paragraf panjang yang menjawab kueri tersebut",
			explain: "Benar!",
			correct : true
		},
		{
			text: "Kueri dan paragraf dengan panjang yang hampir sama",
			explain: "Ini adalah contoh dari pencarian semantik simetris -- coba lagi!"
		},
		{
			text: "Kueri panjang dan paragraf pendek yang menjawabnya",
			explain: "Ini salah -- coba lagi!"
		}
	]}
/>

### 10. Dapatkah saya menggunakan 🤗 Datasets untuk memuat data di domain lain, seperti pemrosesan suara?

<Question
	choices={[
		{
			text: "Tidak",
			explain: "Ini salah -- 🤗 Datasets saat ini mendukung data tabular, audio, dan visi komputer. Lihat <a href='https://huggingface.co/datasets/mnist'>dataset MNIST</a> untuk contoh visi komputer."
		},
		{
			text: "Ya",
			explain: "Benar! Lihat perkembangan menarik dalam bidang suara dan visi di pustaka 🤗 Transformers untuk melihat bagaimana 🤗 Datasets digunakan dalam domain ini.",
			correct : true
		},
	]}
/>

<CourseFloatingBanner chapter={2}
  classNames="absolute z-10 right-0 top-0"
  notebooks={[
    {label: "Google Colab", value: "https://colab.research.google.com/github/huggingface/notebooks/blob/main/course/en/chapter12/grpo_finetune.ipynb"},
]} />

# പ്രായോഗിക അഭ്യാസം: GRPO ഉപയോഗിച്ച് ഒരു മോഡൽ ഫൈൻ-ട്യൂൺ ചെയ്യുക

സിദ്ധാന്തം കണ്ടുകഴിഞ്ഞല്ലോ, ഇനി അത് പ്രയോഗത്തിൽ വരുത്താം! ഈ അഭ്യാസത്തിൽ, GRPO ഉപയോഗിച്ച് നിങ്ങൾ ഒരു മോഡൽ ഫൈൻ-ട്യൂൺ ചെയ്യും.

> [!TIP]
> ഈ അഭ്യാസം എഴുതിയത് LLM ഫൈൻ-ട്യൂണിംഗ് വിദഗ്ധനായ [@mlabonne](https://huggingface.co/mlabonne) ആണ്.

## ഡിപ്പൻഡൻസികൾ ഇൻസ്‌റ്റാൾ ചെയ്യുക

ആദ്യം, ഈ അഭ്യാസത്തിനുള്ള ഡിപ്പൻഡൻസികൾ ഇൻസ്‌റ്റാൾ ചെയ്യാം.

```bash
!pip install -qqq datasets==3.2.0 transformers==4.47.1 trl==0.14.0 peft==0.14.0 accelerate==1.2.1 bitsandbytes==0.45.2 wandb==0.19.7 --progress-bar off
!pip install -qqq flash-attn --no-build-isolation --progress-bar off
```

ഇപ്പോൾ ആവശ്യമായ ലൈബ്രറികൾ ഇംപോർട്ട് ചെയ്യാം.

```python
import torch
from datasets import load_dataset
from peft import LoraConfig, get_peft_model
from transformers import AutoModelForCausalLM, AutoTokenizer
from trl import GRPOConfig, GRPOTrainer
```

## Weights & Biases ഇംപോർട്ട് ചെയ്ത് ലോഗിൻ ചെയ്യുക

നിങ്ങളുടെ പരീക്ഷണങ്ങൾ ലോഗ് ചെയ്യാനും നിരീക്ഷിക്കാനുമുള്ള ഒരു ടൂളാണ് Weights & Biases. നമ്മുടെ ഫൈൻ-ട്യൂണിംഗ് പ്രക്രിയ ലോഗ് ചെയ്യാൻ നമ്മൾ ഇത് ഉപയോഗിക്കും.

```python
import wandb

wandb.login()
```

Weights & Biases-ൽ ലോഗിൻ ചെയ്യാതെയും ഈ അഭ്യാസം ചെയ്യാം, എന്നാൽ നിങ്ങളുടെ പരീക്ഷണങ്ങൾ ട്രാക്ക് ചെയ്യാനും ഫലങ്ങൾ വ്യാഖ്യാനിക്കാനും ലോഗിൻ ചെയ്യുന്നത് ശുപാർശ ചെയ്യുന്നു.

## ഡേറ്റാസെറ്റ് ലോഡ് ചെയ്യുക

ഇപ്പോൾ, ഡേറ്റാസെറ്റ് ലോഡ് ചെയ്യാം. ഈ സന്ദർഭത്തിൽ, ചെറുകഥകളുടെ ഒരു ലിസ്‌റ്റ് ഉൾക്കൊള്ളുന്ന [`mlabonne/smoltldr`](https://huggingface.co/datasets/mlabonne/smoltldr) ഡേറ്റാസെറ്റ് നമ്മൾ ഉപയോഗിക്കും.

```python
dataset = load_dataset("mlabonne/smoltldr")
print(dataset)
```

## മോഡൽ ലോഡ് ചെയ്യുക

ഇപ്പോൾ, മോഡൽ ലോഡ് ചെയ്യാം.

ഈ അഭ്യാസത്തിനായി, [`SmolLM2-135M`](https://huggingface.co/HuggingFaceTB/SmolLM2-135M) മോഡൽ നമ്മൾ ഉപയോഗിക്കും. 

പരിമിതമായ ഹാർഡ്‌വെയറിൽ പ്രവർത്തിക്കുന്ന 135M പാരാമീറ്ററുകളുള്ള ഒരു ചെറിയ മോഡലാണിത്. ഇത് പഠനത്തിന് മോഡലിനെ അനുയോജ്യമാക്കുന്നു, എന്നാൽ ഇത് ലഭ്യമായതിൽ ഏറ്റവും ശക്തമായ മോഡലല്ല. കൂടുതൽ ശക്തമായ ഹാർഡ്‌വെയർ നിങ്ങൾക്ക് ലഭ്യമാണെങ്കിൽ, [`SmolLM2-1.7B`](https://huggingface.co/HuggingFaceTB/SmolLM2-1.7B) പോലുള്ള വലിയ മോഡൽ ഫൈൻ-ട്യൂൺ ചെയ്യാൻ ശ്രമിക്കാം.

```python
model_id = "HuggingFaceTB/SmolLM-135M-Instruct"
model = AutoModelForCausalLM.from_pretrained(
    model_id,
    torch_dtype="auto",
    device_map="auto",
    attn_implementation="flash_attention_2",
)
tokenizer = AutoTokenizer.from_pretrained(model_id)
```

## LoRA ലോഡ് ചെയ്യുക

ഇപ്പോൾ, LoRA കോൺഫിഗറേഷൻ ലോഡ് ചെയ്യാം. ട്രെയിൻ ചെയ്യാവുന്ന പാരാമീറ്ററുകളുടെ എണ്ണം കുറയ്ക്കാനും, അതിലൂടെ മോഡൽ ഫൈൻ-ട്യൂൺ ചെയ്യാൻ ആവശ്യമായ മെമ്മറി ഉപയോഗം കുറയ്ക്കാനും നമ്മൾ LoRA പ്രയോജനപ്പെടുത്തും.

LoRA-യുമായി നിങ്ങൾക്ക് പരിചയമില്ലെങ്കിൽ, [അധ്യായം 11](https://huggingface.co/learn/course/en/chapter11/3)-ൽ അതിനെക്കുറിച്ച് കൂടുതൽ വായിക്കാം.

```python
# Load LoRA
lora_config = LoraConfig(
    task_type="CAUSAL_LM",
    r=16,
    lora_alpha=32,
    target_modules="all-linear",
)
model = get_peft_model(model, lora_config)
print(model.print_trainable_parameters())
```

```sh
Total trainable parameters: 135M
```

## റിവാർഡ് ഫംഗ്‌ഷൻ നിർവചിക്കുക

മുൻ വിഭാഗത്തിൽ സൂചിപ്പിച്ചതുപോലെ, മോഡൽ മെച്ചപ്പെടുത്താൻ GRPO-യ്ക്ക് ഏത് റിവാർഡ് ഫംഗ്‌ഷനും ഉപയോഗിക്കാം. ഈ സന്ദർഭത്തിൽ, 50 ടോക്കണുകൾ ദൈർഘ്യമുള്ള ടെക്‌സ്‌റ്റ് ജനറേറ്റ് ചെയ്യാൻ മോഡലിനെ പ്രോത്സാഹിപ്പിക്കുന്ന ഒരു ലളിതമായ റിവാർഡ് ഫംഗ്‌ഷൻ നമ്മൾ ഉപയോഗിക്കും.

```python
# Reward function
ideal_length = 50


def reward_len(completions, **kwargs):
    return [-abs(ideal_length - len(completion)) for completion in completions]
```

## ട്രെയിനിംഗ് ആർഗ്യുമെന്റുകൾ നിർവചിക്കുക

ഇപ്പോൾ, ട്രെയിനിംഗ് ആർഗ്യുമെന്റുകൾ നിർവചിക്കാം. സാധാരണ `transformers` ശൈലിയിൽ ട്രെയിനിംഗ് ആർഗ്യുമെന്റുകൾ നിർവചിക്കാൻ നമ്മൾ `GRPOConfig` ക്ലാസ് ഉപയോഗിക്കും.

ട്രെയിനിംഗ് ആർഗ്യുമെന്റുകൾ നിർവചിക്കുന്നത് നിങ്ങൾക്ക് ഇത് ആദ്യമായാണെങ്കിൽ, കൂടുതൽ വിവരങ്ങൾക്ക് [TrainingArguments](https://huggingface.co/docs/transformers/en/main_classes/trainer#trainingarguments) ക്ലാസ് പരിശോധിക്കാം, അല്ലെങ്കിൽ വിശദമായ ആമുഖത്തിനായി [അധ്യായം 2](https://huggingface.co/learn/course/en/chapter2/1) നോക്കാം.

```python
# Training arguments
training_args = GRPOConfig(
    output_dir="GRPO",
    learning_rate=2e-5,
    per_device_train_batch_size=8,
    gradient_accumulation_steps=2,
    max_prompt_length=512,
    max_completion_length=96,
    num_generations=8,
    optim="adamw_8bit",
    num_train_epochs=1,
    bf16=True,
    report_to=["wandb"],
    remove_unused_columns=False,
    logging_steps=1,
)
```

ഇപ്പോൾ, മോഡൽ, ഡേറ്റാസെറ്റ്, ട്രെയിനിംഗ് ആർഗ്യുമെന്റുകൾ എന്നിവ ഉപയോഗിച്ച് ട്രെയിനർ ഇനിഷ്യലൈസ് ചെയ്ത് ട്രെയിനിംഗ് ആരംഭിക്കാം.

```python
# Trainer
trainer = GRPOTrainer(
    model=model,
    reward_funcs=[reward_len],
    args=training_args,
    train_dataset=dataset["train"],
)

# Train model
wandb.init(project="GRPO")
trainer.train()
```

Google Colab-ലോ Hugging Face Spaces വഴിയോ ലഭ്യമായ ഒരു A10G GPU-വിൽ ട്രെയിനിംഗിന് ഏകദേശം 1 മണിക്കൂർ എടുക്കും.

## ട്രെയിനിംഗ് സമയത്ത് മോഡൽ Hub-ലേക്ക് പുഷ് ചെയ്യുക

`push_to_hub` ആർഗ്യുമെന്റ് `True` എന്നും `model_id` ആർഗ്യുമെന്റ് സാധുവായ ഒരു മോഡൽ പേരും ആയി സജ്ജമാക്കിയാൽ, ട്രെയിനിംഗ് നടക്കുന്ന സമയത്തുതന്നെ മോഡൽ Hugging Face Hub-ലേക്ക് പുഷ് ചെയ്യപ്പെടും. മോഡൽ ഉടൻ തന്നെ വൈബ് ടെസ്‌റ്റ് ചെയ്ത് തുടങ്ങാൻ നിങ്ങൾ ആഗ്രഹിക്കുന്നുവെങ്കിൽ ഇത് ഉപകാരപ്രദമാണ്!

## ട്രെയിനിംഗ് ഫലങ്ങൾ വ്യാഖ്യാനിക്കുക

നിങ്ങളുടെ റിവാർഡ് ഫംഗ്‌ഷനിൽ നിന്നുള്ള റിവാർഡ്, ലോസ്, മറ്റ് പല മെട്രിക്കുകളും `GRPOTrainer` ലോഗ് ചെയ്യുന്നു.

റിവാർഡ് ഫംഗ്‌ഷനിൽ നിന്നുള്ള റിവാർഡിലും ലോസിലുമാണ് നമ്മൾ ശ്രദ്ധ കേന്ദ്രീകരിക്കുന്നത്.

നിങ്ങൾക്ക് കാണാൻ കഴിയുന്നതുപോലെ, മോഡൽ പഠിക്കുന്നതിനനുസരിച്ച് റിവാർഡ് ഫംഗ്‌ഷനിൽ നിന്നുള്ള റിവാർഡ് 0-ത്തോട് അടുക്കുന്നു. ശരിയായ ദൈർഘ്യമുള്ള ടെക്‌സ്‌റ്റ് ജനറേറ്റ് ചെയ്യാൻ മോഡൽ പഠിക്കുന്നു എന്നതിന്റെ നല്ല സൂചനയാണിത്.

![റിവാർഡ് ഫംഗ്‌ഷനിൽ നിന്നുള്ള റിവാർഡ്](https://huggingface.co/reasoning-course/images/resolve/main/grpo/13.png)

ലോസ് പൂജ്യത്തിൽ തുടങ്ങി ട്രെയിനിംഗ് സമയത്ത് വർദ്ധിക്കുന്നത് നിങ്ങൾ ശ്രദ്ധിച്ചേക്കാം; ഇത് വിപരീതമായി തോന്നാം. ഈ പെരുമാറ്റം GRPO-യിൽ പ്രതീക്ഷിക്കുന്നതാണ്, അൽഗോരിതത്തിന്റെ ഗണിതശാസ്ത്ര രൂപീകരണവുമായി ഇത് നേരിട്ട് ബന്ധപ്പെട്ടിരിക്കുന്നു. GRPO-യിലെ ലോസ് KL ഡൈവർജൻസിന് (യഥാർത്ഥ പോളിസിയോട് ആപേക്ഷികമായ പരിധി) ആനുപാതികമാണ്. ട്രെയിനിംഗ് പുരോഗമിക്കുമ്പോൾ, റിവാർഡ് ഫംഗ്‌ഷനുമായി കൂടുതൽ യോജിക്കുന്ന ടെക്‌സ്‌റ്റ് ജനറേറ്റ് ചെയ്യാൻ മോഡൽ പഠിക്കുന്നു; ഇത് അതിന്റെ പ്രാരംഭ പോളിസിയിൽ നിന്ന് കൂടുതൽ വ്യതിചലിക്കാൻ കാരണമാകുന്നു. ഈ വർദ്ധിച്ചുവരുന്ന വ്യതിയാനമാണ് ഉയരുന്ന ലോസ് മൂല്യത്തിൽ പ്രതിഫലിക്കുന്നത്; റിവാർഡ് ഫംഗ്‌ഷനായി ഒപ്റ്റിമൈസ് ചെയ്യാൻ മോഡൽ വിജയകരമായി അഡാപ്‌റ്റ് ചെയ്യുന്നു എന്നാണ് യഥാർത്ഥത്തിൽ ഇത് സൂചിപ്പിക്കുന്നത്.

![ലോസ്](https://huggingface.co/reasoning-course/images/resolve/main/grpo/14.png)

## മോഡൽ സേവ് ചെയ്ത് പ്രസിദ്ധീകരിക്കുക

മോഡൽ കമ്മ്യൂണിറ്റിയുമായി പങ്കുവയ്ക്കാം!

```python
merged_model = trainer.model.merge_and_unload()
merged_model.push_to_hub(
    "SmolGRPO-135M", private=False, tags=["GRPO", "Reasoning-Course"]
)
```

## ടെക്‌സ്‌റ്റ് ജനറേറ്റ് ചെയ്യുക

🎉 GRPO ഉപയോഗിച്ച് നിങ്ങൾ വിജയകരമായി ഒരു മോഡൽ ഫൈൻ-ട്യൂൺ ചെയ്തു! ഇപ്പോൾ, മോഡൽ ഉപയോഗിച്ച് കുറച്ച് ടെക്‌സ്‌റ്റ് ജനറേറ്റ് ചെയ്യാം.

ആദ്യം, വളരെ നീണ്ട ഒരു ഡോക്യുമെന്റ് നമ്മൾ നിർവചിക്കും!

```python
prompt = """
# A long document about the Cat

The cat (Felis catus), also referred to as the domestic cat or house cat, is a small 
domesticated carnivorous mammal. It is the only domesticated species of the family Felidae.
Advances in archaeology and genetics have shown that the domestication of the cat occurred
in the Near East around 7500 BC. It is commonly kept as a pet and farm cat, but also ranges
freely as a feral cat avoiding human contact. It is valued by humans for companionship and
its ability to kill vermin. Its retractable claws are adapted to killing small prey species
such as mice and rats. It has a strong, flexible body, quick reflexes, and sharp teeth,
and its night vision and sense of smell are well developed. It is a social species,
but a solitary hunter and a crepuscular predator. Cat communication includes
vocalizations—including meowing, purring, trilling, hissing, growling, and grunting—as
well as body language. It can hear sounds too faint or too high in frequency for human ears,
such as those made by small mammals. It secretes and perceives pheromones.
"""

messages = [
    {"role": "user", "content": prompt},
]
```

ഇപ്പോൾ, മോഡൽ ഉപയോഗിച്ച് നമുക്ക് ടെക്‌സ്‌റ്റ് ജനറേറ്റ് ചെയ്യാം.

```python
# Generate text
from transformers import pipeline

generator = pipeline("text-generation", model="SmolGRPO-135M")

## Or use the model and tokenizer we defined earlier
# generator = pipeline("text-generation", model=model, tokenizer=tokenizer)

generate_kwargs = {
    "max_new_tokens": 256,
    "do_sample": True,
    "temperature": 0.5,
    "min_p": 0.1,
}

generated_text = generator(messages, generate_kwargs=generate_kwargs)

print(generated_text)
```

# ഉപസംഹാരം

ഈ അധ്യായത്തിൽ, GRPO ഉപയോഗിച്ച് ഒരു മോഡൽ എങ്ങനെ ഫൈൻ-ട്യൂൺ ചെയ്യാം എന്ന് നമ്മൾ കണ്ടു. ട്രെയിനിംഗ് ഫലങ്ങൾ എങ്ങനെ വ്യാഖ്യാനിക്കാമെന്നും മോഡൽ ഉപയോഗിച്ച് ടെക്‌സ്‌റ്റ് എങ്ങനെ ജനറേറ്റ് ചെയ്യാമെന്നും നമ്മൾ കണ്ടു.
